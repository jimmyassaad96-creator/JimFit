// @generated by tools/extract/extract.py — bodies copied verbatim from index.html

  // ---- push notifications (Sept 2026) ----
  // Real background push — "your program is ready", "your diet plan is
  // back", "you're all set" — instead of a client only finding out the next
  // time they happen to open the app. This is the PUBLIC half of a VAPID key
  // pair; the matching private key lives only in the Supabase Edge Function
  // that actually sends pushes (see supabase_push_notifications.sql / the
  // send-push function in the roadmap doc), never in this client-side file.
  // Regenerating this key would silently un-subscribe every device that
  // already opted in, so treat it as a one-time value, not something to
  // casually swap out.
  const VAPID_PUBLIC_KEY = "BAq4QH6tWkKPK9GaXaKTPV0rnwZhoYeZ6YmvaFnmcl5v8zwR5cK4Fzt4VDoEFYV8g6tpUjCZlV1MwQGAoiTmDSw";

  const PUSH_SUPPORTED = typeof window !== "undefined" && "serviceWorker" in navigator && "PushManager" in window && "Notification" in window;

export { VAPID_PUBLIC_KEY, PUSH_SUPPORTED };
